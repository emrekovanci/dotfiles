* Install (in admin mode)

```powershell
wsl --install
```

* Available Distros

```powershell
wsl --list --online
```

* Running vhosts

```powershell
wsl --list --running
```

* All vshosts

```powershell
wsl --list --verbose
```

* Terminate

```powershell
wsl -t Ubuntu
```
